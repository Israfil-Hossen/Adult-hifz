# সেটআপ ও রক্ষণাবেক্ষণ

এই ফাইলটা আপনার নিজের জন্য — যিনি অ্যাপটা চালাচ্ছেন। সাধারণ ব্যবহারকারীর এটা লাগবে না।

**আপনার অ্যাপের ঠিকানা:** https://israfil-hossen.github.io/Adult-hifz/
**আপনার repo:** `israfil-hossen/Adult-hifz`

---

## ✅ যা ইতিমধ্যে হয়ে গেছে

- সাতটা ফাইল repo-র root-এ আপলোড হয়েছে
- GitHub Pages চালু, ওয়েবসাইট কাজ করছে
- অ্যাপ ইনস্টলযোগ্য, ইন্টারনেট ছাড়াও চলে
- ডেটা ব্যবহারকারীর ডিভাইসে জমা হচ্ছে

**এখন শুধু বাকি:** Google Drive সিঙ্ক চালু করা (নিচে ধাপ ১)।

---

# ধাপ ১ · Google Drive সিঙ্ক চালু করা

এটা চালু না করলেও অ্যাপ চলবে, তবে ব্রাউজার পরিষ্কার করলে ডেটা হারাবে। চালু করলে ডেটা আপনার নিজের ড্রাইভে জমবে, আর ফোন-ল্যাপটপে একই থাকবে।

**সময় দশ মিনিট। খরচ নেই। একবারই করতে হবে।**

## ১ক · Google Cloud-এ প্রকল্প বানান

১. [console.cloud.google.com](https://console.cloud.google.com/) এ যান, Google দিয়ে লগ ইন করুন

২. উপরে প্রকল্পের নামের জায়গায় ক্লিক → **New Project** → নাম দিন `Hifz` → **Create**

৩. উপরের সার্চ বাক্সে লিখুন **Google Drive API** → খুলে **Enable** চাপুন

## ১খ · সম্মতির পর্দা সাজান

৪. বাঁ মেনু → **APIs & Services** → **OAuth consent screen**
   *(নতুন কনসোলে এর নাম **Google Auth Platform → Branding**)*

৫. **External** বেছে নিন → **Create**

৬. তিনটা ঘর ভরুন, বাকিগুলো খালি রাখলেও চলবে:
   - **App name**: `হিফজ দৈনিক সঙ্গী`
   - **User support email**: আপনার ইমেইল
   - **Developer contact**: আপনার ইমেইল

৭. Save করে এগোতে থাকুন → **Back to dashboard**

৮. ড্যাশবোর্ডে **PUBLISH APP** বোতাম থাকলে চাপুন → **Confirm**

> **কেন জরুরি:** না চাপলে সর্বোচ্চ ১০০ জন ব্যবহার করতে পারবে। আপনি যেহেতু ছড়াতে চাইছেন, এটা লাগবেই। এই অ্যাপ শুধু `drive.file` অনুমতি চায় — Google-এর "সংবেদনশীল নয়" শ্রেণির, তাই কোনো যাচাই বা নিরীক্ষা লাগে না।

## ১গ · Client ID বানান

৯. **APIs & Services** → **Credentials** → **+ CREATE CREDENTIALS** → **OAuth client ID**

১০. **Application type**: `Web application`

১১. **Authorized JavaScript origins** এ **+ ADD URI** চেপে **হুবহু এটা** বসান:

```
https://israfil-hossen.github.io
```

> ⚠️ **সবচেয়ে বেশি ভুল এখানেই হয়।**
> - `/Adult-hifz/` অংশটা **দেবেন না** — শুধু ডোমেইনটুকু
> - শেষে স্ল্যাশ (`/`) দেবেন না
> - সব ছোট হাতের অক্ষর — `israfil-hossen`, `Israfil-Hossen` নয়
> - **Redirect URI**-এর ঘরটা **খালি রাখুন**, ওটা লাগবে না

১২. **Create** চাপুন। লম্বা একটা লেখা দেখাবে, শেষে `.apps.googleusercontent.com`। **কপি করুন।**

## ১ঘ · অ্যাপে বসান

১৩. GitHub-এ যান → https://github.com/israfil-hossen/Adult-hifz → `config.js` ফাইলে ক্লিক → পেন্সিল আইকন (✏️)

১৪. এই লাইনটা খুঁজুন:

```js
googleClientId: "",
```

কপি করা লেখাটা কোটেশনের ভিতরে বসান:

```js
googleClientId: "8281xxxxxxxx-xxxxxxxxxx.apps.googleusercontent.com",
```

১৫. নিচে **Commit changes** → আবার **Commit changes**

১৬. একই সাথে `sw.js` ফাইলটাও এডিট করে `hifz-v2` কে `hifz-v3` করে দিন *(নাহলে পুরোনো সংস্করণ ক্যাশে আটকে থাকবে)*

১৭. দুই মিনিট পর অ্যাপ খুলুন। উপরে **সিঙ্ক** বোতাম দেখা যাবে। সেটিংস ট্যাব → **Google দিয়ে যুক্ত হোন**

---

## সিঙ্ক নিয়ে কয়েকটা কথা

**অ্যাপ ড্রাইভের কী দেখতে পায়?** শুধু নিজের বানানো `hifz-backup.json` ফাইলটা। আপনার ছবি, ডকুমেন্ট, বাকি কিছুই নয়। এটা Google-এর দিক থেকেই আটকানো।

**অন্যরা ব্যবহার করলে তাদের ডেটা কোথায় যায়?** যার যার নিজের ড্রাইভে। আপনি কিছুই দেখতে পাবেন না। শুধু সম্মতির পর্দায় আপনার প্রকল্পের নামটা দেখাবে।

**দুই ডিভাইসে কাজ করলে?** যেটায় শেষে পরিবর্তন হয়েছে সেটাই থাকবে। এক ডিভাইসে শেষ করে অন্যটা খুলুন — খুললেই নতুনটা নিজে নেমে আসবে।

**"Google hasn't verified this app" এলে?** **Advanced** → **Go to … (unsafe)**। অ্যাপটা আপনারই, আর কোনো সংবেদনশীল অনুমতি চায় না।

**Gmail-এ অটো ইমেইল কেন নেই?** ওটার জন্য `gmail.send` অনুমতি লাগে, যেটা "restricted" শ্রেণির — প্রতি বছর কয়েক হাজার ডলারের নিরাপত্তা নিরীক্ষা। ড্রাইভ সিঙ্কে একই সুরক্ষা মিলছে, তাই ওই খরচের মানে নেই। চাইলে সেটিংস থেকে JSON নামিয়ে নিজেকে ইমেইল করতে পারেন।

---

# ধাপ ২ · repo-টা সাজান

GitHub-এ repo-র পাতায় ডান পাশে **About**-এর ⚙️ চেপে:

- **Description**: `প্রাপ্তবয়স্কদের জন্য গবেষণা-ভিত্তিক কোরআন হিফজ ট্র্যাকার`
- **Website**: `https://israfil-hossen.github.io/Adult-hifz/`
- **Topics**: `quran` `hifz` `memorization` `bangla` `bengali` `pwa` `islam`

**স্ক্রিনশট যোগ করুন।** ফোন থেকে "আজ" আর "অগ্রগতি" ট্যাবের দুটো স্ক্রিনশট নিন, repo-তে `screenshot-today.png` আর `screenshot-progress.png` নামে আপলোড করুন, তারপর README-র শুরুতে বসিয়ে দিন:

```markdown
<img src="screenshot-today.png" width="270"> <img src="screenshot-progress.png" width="270">
```

মানুষ ছবি দেখে যত দ্রুত বোঝে, লেখা পড়ে তত না।

---

# ধাপ ৩ · মানুষের মাঝে ছড়ানো

## কপি করে পাঠানোর মতো বার্তা

```
আসসালামু আলাইকুম।

বড় বয়সে যারা হিফজ শুরু করতে চান, তাদের জন্য একটা ছোট
অ্যাপ বানিয়েছি। বিনামূল্যে, বিজ্ঞাপন নেই, লগ ইন ছাড়াই চলে।

প্রতিদিন খুললেই বলে দেয় — আজ কোন পৃষ্ঠা নতুন, কোন সাতটা
পৃষ্ঠা রিভিশন, আর কোন পারা মুরাজাআ। হিসাব রাখতে হয় না।

👉 https://israfil-hossen.github.io/Adult-hifz/

ফোনে খুলে মেনু থেকে "Add to Home Screen" দিলে অ্যাপের মতো
বসে যাবে, ইন্টারনেট ছাড়াও চলবে।
```

> **ইনস্টলের কথাটা বাদ দেবেন না।** বেশিরভাগ মানুষ লিংক খুলে দেখে, তারপর ভুলে যায়। "হোম স্ক্রিনে যোগ করুন" না বললে অ্যাপটা অ্যাপ হয়ে ওঠে না।

## QR কোড

মসজিদে বা মাদরাসায় কাগজে ছাপিয়ে দিতে পারেন। যেকোনো ফ্রি QR জেনারেটরে আপনার লিংকটা দিলেই হবে। মুখে লিংক বলার চেয়ে এটা অনেক ভালো কাজ করে।

## যাদের কাছে দেবেন

সবচেয়ে ভালো ফল পাবেন — মসজিদের ইমাম ও খতিব, মাদরাসার শিক্ষক, প্রাপ্তবয়স্কদের হিফজ হালাকা, আর কর্মজীবী মানুষের ইসলামিক গ্রুপে।

শিক্ষকদের **সাপ্তাহিক রিপোর্ট** ফিচারটার কথা আলাদা করে বলবেন — ছাত্রের সপ্তাহের পুরো হিসাব এক পাতায় প্রিন্ট হয়ে আসে, নিচে মন্তব্য আর স্বাক্ষরের ঘরসহ। এটাই তাদের কাছে সবচেয়ে কাজের জিনিস।

---

# অ্যাপ আপডেট করা

GitHub-এ সরাসরি ফাইল এডিট করতে পারেন — repo-তে ফাইলে ক্লিক → পেন্সিল আইকন → বদল → **Commit changes**।

**প্রতিবার আপডেটের পর `sw.js` ফাইলের এই লাইনটা বদলাতে ভুলবেন না:**

```js
const CACHE = "hifz-v3";   // পরেরবার v4, তারপর v5 …
```

না বদলালে যাদের কাছে অ্যাপ ইনস্টল করা আছে, তারা পুরোনো সংস্করণই দেখতে থাকবে।

## কোথায় কী আছে

| কী বদলাবেন | কোন ফাংশনে |
|---|---|
| রঙ, ফন্ট | ফাইলের শুরুর `:root` ব্লক |
| র‍্যাম্পের ধাপ (৫→৮→১১→১৫) | `targetLines()` |
| সাবকিতে কয় দিন | `sabaqiQueue()` |
| মানজিলের ঘূর্ণন | `manzilJuz()` |
| ব্লকগুলোর ড্রিল-লেখা | `renderToday()` |
| রুটিন ট্যাবের সব লেখা | `renderRoutine()` |
| সাপ্তাহিক রিপোর্ট | `openReport()` |
| ড্রাইভ সিঙ্ক | `Sync` অবজেক্ট |

---

# সমস্যা হলে

| যা হচ্ছে | কারণ ও সমাধান |
|---|---|
| সিঙ্ক বোতামই দেখাচ্ছে না | `config.js`-এ Client ID বসেনি, বা কোটেশন চিহ্ন মুছে গেছে |
| `origin_mismatch` / `redirect_uri_mismatch` | Authorized JavaScript origins এ `/Adult-hifz/` ঢুকে গেছে। শুধু `https://israfil-hossen.github.io` রাখুন |
| যুক্ত হয় কিন্তু সিঙ্ক হয় না | Google Drive API **Enable** করা হয়নি |
| ১০০ জনের পর কেউ ঢুকতে পারছে না | সম্মতির পর্দা এখনো Testing-এ। **PUBLISH APP** চাপুন |
| আপডেট করেছি কিন্তু পুরোনোটাই দেখাচ্ছে | `sw.js`-এর CACHE সংখ্যা বাড়াননি |
| আইফোনে ইনস্টল হচ্ছে না | Chrome-এ চেষ্টা করছেন। আইফোনে অবশ্যই **Safari** |
| ওয়েবসাইট খুলছে না | Settings → Pages এ Branch `main` আর `/ (root)` আছে কি না দেখুন |
| ফাইল আছে কিন্তু ডিজাইন ভাঙা | ফাইলগুলো সাব-ফোল্ডারে চলে গেছে। সব root-এ থাকতে হবে |

---

# ফাইলগুলো কী কাজ করে

| ফাইল | কাজ |
|---|---|
| `index.html` | পুরো অ্যাপ — কোড, ডিজাইন, রুটিনের সব লেখা |
| `config.js` | আপনার Google Client ID |
| `manifest.json` | ফোনে অ্যাপ হিসেবে বসার তথ্য |
| `sw.js` | অফলাইন ক্যাশ |
| `icon.svg` | আইকন |
| `icon-maskable.svg` | অ্যান্ড্রয়েডের গোল আইকন |
| `README.md` | repo-র সামনের পাতা — দর্শনার্থীরা এটা দেখে |
| `SETUP.md` | এই ফাইল — শুধু আপনার জন্য |
